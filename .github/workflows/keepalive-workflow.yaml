name: Keep All Workflow Alive

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  keepalive-workflow:
    name: Keepalive workflow
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - uses: actions/checkout@v4
      - uses: gautamkrishnar/keepalive-workflow@v2